secrets:
  cloudpi_secrets:
    file: /run/secrets-tmp/cloudpi.secrets

services:
  db:
    image: cloudpi1/cloudpi:Cloudpi_db_v1.1.044
    container_name: cloudpi-db
    ports: 
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    restart: always
    networks:
      - cloudpi-network
#    secrets:
#      - cloudpi_secrets
#      - db_password
#      - db_root_password
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "mysqladmin ping -h localhost || exit 1",
        ]
      interval: 10s
      timeout: 20s
      retries: 10
      start_period: 90s

    env_file:
      - .env
      - /run/secrets-tmp/cloudpi.secrets
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - SETGID
      - SETUID
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 8G

  app:
    image: cloudpi1/cloudpi:Cloudpi_v1.1.044
    container_name: cloudpi-app
    hostname: cloudpi-app
    restart: always
    depends_on:
      db:
        condition: service_healthy
    secrets:
      - cloudpi_secrets
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost/CPiP/v1/health && curl -f http://localhost/CPiN/monitor"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    ports:
      - "443:443" # HTTPS
      - "80:80" # HTTP
    networks:
      - cloudpi-network
    env_file:
      - .env
      - /run/secrets-tmp/cloudpi.secrets
    environment:
      HOST: ${HOST:-localhost}
      OPENBLAS_NUM_THREADS: "2"
      OMP_NUM_THREADS: "2"
    volumes:
      - redis_data:/var/lib/redis
      - ./certs:/home/certs
      - cloudpi:/app/backups  # T043: Persistent volume for migration backups
    security_opt:
      - no-new-privileges:true
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 8G

volumes:
  mysql_data:
    driver: local
  redis_data:
    driver: local
  cloudpi:          # T043: Persistent volume for migration backups (survives container restarts)
    driver: local

networks:
  cloudpi-network:
    driver: bridge
    # SECURITY: Enable network isolation
    internal: false  # Set to true if no external access needed
    ipam:
      config:
        - subnet: 172.28.0.0/16
